<p id="notice"><%= notice %></p><br>

<h1>Les entrées de </h1><br>


<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Name </th>
      <th>Montant in</th>
      <th>Motif</th>

      <th colspan="3"></th>
    </tr>
  </thead><br>

  <tbody>
    <% @entres.each do |entre| %>
      <tr>
        <td><%= entre.date %></td>
        <td><%= entre.user.name %></td>
        <td><%= entre.montant_in %></td>
        <td><%= entre.motif %></td>
        <td><%= link_to 'Show', entre %></td>
        <td><%= link_to 'Edit', edit_entre_path(entre) %></td>
        <td><%= link_to 'Destroy', entre, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr><br>
    <% end %>

  </tbody>
</table>

<br>

<% somme_entre = 0 %>
<% @entres.each do |entre| %>
  <% somme_entre += entre.montant_in.to_i %>
<% end %>
<p> La somme des entrees : <%= somme_entre %></p><br>

<%= link_to 'New Entre', new_entre_path %>
